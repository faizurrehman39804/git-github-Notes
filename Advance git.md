# Git Github

# Qusetion -->

1: just 3 file pa commet karana ha na ka all file per

frist time poch the code add this cammand.

# 1:add the name in brach:-
# 2: reverse form staging state:-



# 1:- add the name in brach -->

1: `git config --global user.name` "Faiz"
2: `git config --global user.name` "faizurrehman@gmil.com"

# 2: reverse the poch file.
1: `git add .` {staging ma lana ka lia (poch karana ka lia)}
2: `git restore --staged` {staging area sa nikalana ka lia(per ya poch nahi ho gha githup per)}

# Braching: "Create brach","delete brach","swicth barach"


# 1: Check the Brach:
`git branch` {see the all braches for cheking}.

# 2: Create a Brach:
`git Brach (Brach name)` {create a new barach}.

# 3: Switch Brach:
`git swicth (brach name)` {switch to another brach}.

# 4: merge the Brach:
`git merge (barach name then merge)` 
REQUREMENT: {pahala main brach ma jahana ha per merge karana ha barach ko}.
